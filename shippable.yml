language: java

jdk:
   - openjdk7

before_install:
   - mvn clean dependency:copy-dependencies
   - mkdir /root/.m2/repository/ie/nuigalway/mfinn/StudentRegistration/1.0.0/StudentRegistration-1.0.0.jar
 
install:
   - mvn compile assembly:single

notifications:
  email:
      recipients:
         - m.finn11@nuigalway.ie